/*function changeColorAndImage() {
    var image = document.getElementById("myImage");
    var colors = ["red", "green", "blue", "orange", "purple"];
    var currentColor = image.style.backgroundColor || "white"; // Початковий колір або білий, якщо не вказано

    // Змінюємо колір на випадковий інший, якщо є більше кольорів
    var newColor = colors[Math.floor(Math.random() * colors.length)];
    while (newColor === currentColor) {
        newColor = colors[Math.floor(Math.random() * colors.length)];
    }

    image.style.backgroundColor = newColor;
}
*/